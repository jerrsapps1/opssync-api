# Copy to .env in Render (Environment → Variables) or local dev
NODE_ENV=development
# Render provides PORT automatically – do not set it here in production
ALLOWED_ORIGINS=https://opssync.ai,https://app.opssync.ai
